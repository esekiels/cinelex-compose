<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Cinelex" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.Cinelex.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="windowSplashScreenBackground">@color/cinelexPrimary</item>
        <item name="postSplashScreenTheme">@style/Theme.Cinelex</item>
    </style>
</resources>
